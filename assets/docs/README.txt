resume.pdf
  The public, web-safe version of Anika's CV. Generated from the original
  Word file, with two changes and nothing else:
      - home address  ->  "Manila, Philippines"
      - mobile number ->  removed (body and page footer)
  Everything else — layout, wording, formatting — is exactly as written.

  Keep the unredacted original OFF this repo. This folder is published to
  the public web; anything dropped in here is downloadable by anyone.
  Send the full version privately to actual employers instead.

  To regenerate after editing your CV: send the new file and it can be
  re-redacted and re-exported the same way.

Other files
  Anything else you add here can be linked with a relative path, e.g.
      <a href="assets/docs/certificate.pdf">Certificate</a>
